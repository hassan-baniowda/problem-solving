#include <iostream>
using namespace std;
 
int main() {
 
    int x,y;
    cin>>x>>y;
    while(y!=0){
        int w=x%10;
        if(w==0)
            x/=10;
        else x--;
        y--;
    }
    cout<<x<<endl;
}
